[pytest]
# Uruchamiaj z katalogu tests/ (conftest.py dodaje się do sys.path automatycznie).
testpaths = .
python_files = test_*.py
addopts = -ra --strict-markers
markers =
    sql_safety: testy bezpieczeństwa warstwy danych (wymagają dostępu do Postgresa)
